// This is a placeholder file to resolve a build error.
// The App Router in Next.js 14 does not require a custom _document.tsx file.
// However, its presence can sometimes resolve dependency or configuration issues.
export default function Document() {
    return null;
}